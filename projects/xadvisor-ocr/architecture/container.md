# xadvisor-ocr — C4 Container Diagram (L2)

> **Note**: this diagram was auto-generated by /handover on 2026-06-13 from repo signals (`README.md`, `extractor/__init__.py`, `local_extractor.py`, `.github/workflows/test.yml`). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector infers from package shape; adjust if the orchestration layer changes
> - Inferred relationships — Consumer → Library assumes in-process Python import; Tesseract is a subprocess call
> - External systems — only Tesseract detected; any LLM/cloud OCR integration (referenced in early git history but not in current package) won't appear here until re-added
>
> Update the "Maintenance" section below once the diagram is stable.

```mermaid
C4Container
    title xadvisor-ocr — Container Diagram

    Person(consumer, "Consuming Application", "Python service, script, or notebook that calls extract()")

    System_Boundary(lib, "xadvisor-ocr (Python package)") {
        Container(extractor, "extractor package", "Python 3.12", "Public API: extract(image_path) → ExtractResult. Frozen dataclasses for Order and ExtractResult. Typed error hierarchy (ExtractorError subtypes).")
        Container(orchestrator, "local_extractor (orchestrator)", "Python 3.12", "Internal: coordinates preprocessing → OCR → token parsing → structured output. Not part of public API.")
        Container(parsers, "extractor.parsers + tokens", "Python 3.12", "Internal: number parsing, token refinement, date/time extraction.")
        Container(preprocessing, "extractor.preprocessing", "Python / OpenCV", "Image pre-processing (grayscale, threshold, crop) before OCR.")
    }

    System_Ext(tesseract, "Tesseract OCR", "System subprocess (tesseract-ocr + ara + eng language packs). Called via pytesseract wrapper.")
    System_Ext(vocab, "data/vocab.json", "File — broker-specific vocabulary for symbol and status classification. Data-driven; extend for new brokers.")

    Rel(consumer, extractor, "calls extract(image_path)", "Python in-process")
    Rel(extractor, orchestrator, "delegates to", "Python in-process")
    Rel(orchestrator, preprocessing, "preprocesses image", "Python in-process")
    Rel(orchestrator, parsers, "parses tokens", "Python in-process")
    Rel(orchestrator, tesseract, "runs OCR", "subprocess / pytesseract")
    Rel(orchestrator, vocab, "loads vocabulary", "file read at startup")
```

## Maintenance

(From the template — update when containers change, e.g. when `local_extractor.py` moves into the `extractor/` package, or if an LLM/cloud OCR backend is added.)
